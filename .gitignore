# Dependencies
node_modules/

# Builds
dist/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Prisma generated client (regenerated via `prisma generate`)
backend/src/generated/

# Environment
.env
.env.local
.env.*.local

# Databases & logs
*.db
*.db.bak
*.log
backend/backend-dev.log
backend/backend-dev.err.log
frontend/frontend-dev.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Stray root artifacts (не проект — только корень, не backend/frontend src)
/src/
/public/
skills-lock.json
~$*.docx
~$*.xlsx

# Freebuff local artifacts
.freebuff/
.freebuff-preview.log

# Local S3-compatible tooling (standalone MinIO binary for e2e) + uploads
.tools/
backend/uploads/
uploads/

# DR backup artifacts (Step 2.17A) — never commit dumps/checksums/evidence
backend/.backups/
*.dump
*.dump.sha256

# Step 2.17B perf harness run artifacts (generated, bulk) — never commit by default
backend/artifacts/performance/
login.json
