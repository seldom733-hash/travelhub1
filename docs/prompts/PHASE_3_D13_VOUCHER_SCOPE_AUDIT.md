# PHASE 3 — D13 — VOUCHER
## SCOPE / REQUALIFICATION AUDIT PROMPT

### 1. Режим выполнения

**Stage:** D13 — Voucher  
**Mode:** Audit-first / Read-only  
**Current TRUE NEXT:** D13

Canonical roadmap identifies D13 as:

```text
D13 — Voucher
Type: IMPLEMENTATION_DEBT
Dependency: D4
Closure: D13
```

D14 depends on completion of D13, а STEP 3.12 остаётся blocked by D14. D12 уже закрыт; D13 не должен переоткрывать D10/D11/D12. fileciteturn324file2L147-L166

### КРИТИЧЕСКОЕ ПРАВИЛО

Это **только Scope / Requalification Audit**.

До завершения audit запрещено:

- менять production code;
- менять Prisma schema;
- создавать Voucher model;
- создавать Voucher API;
- создавать Voucher UI;
- изменять Booking lifecycle;
- изменять Passenger/Traveler semantics;
- изменять Finance;
- изменять D10/D11/D12;
- начинать D14.

После создания evidence report агент обязан остановиться.

---

# 2. PRIMARY QUESTION

Установить:

> Что именно означает canonical Voucher в текущей архитектуре TravelHub, из каких authoritative данных он создаётся, в какой момент lifecycle он появляется, кто является holder/subject Voucher, какие данные должны попасть в Voucher, как обеспечивается неизменяемость опубликованной версии, что означает regeneration/invalidation, какие permissions и scope действуют, какие API/UI targets уже существуют, и готов ли D13 к безопасной implementation.

Нельзя сводить D13 к:

```text
"сделать PDF после BookingConfirmed"
```

Нужно исследовать полноценный domain contract:

```text
Booking
→ Passengers / Booking Travelers
→ Voucher
→ Document / version
→ delivery / access
```

---

# 3. CANONICAL EVIDENCE ALREADY KNOWN

В canonical architecture зафиксировано:

```text
Documents owns Voucher.
Voucher business ID = VCH-*.
Voucher is generated by event/command.
Example:
BookingConfirmed
→ Generate Voucher
```

Documents не меняет Booking/Finance, а опубликованные document versions являются immutable; корректировка создаёт новую версию. fileciteturn326file1L321-L342

Также architecture audit формулирует canonical source candidate:

```text
Booking
→ Booking Travelers / Passengers
→ Voucher
```

и прямо требует определить:

```text
source fields
generation point
regeneration
invalidation
sensitive data
permissions
```

Customer нельзя использовать как автоматический список travelers. fileciteturn326file0L114-L135

Это **starting evidence**, а не заранее завершённый D13 contract.

---

# 4. AUTHORITY ORDER

Использовать:

1. Source tree / Git.
2. Tests / implemented contracts / security.
3. Prisma schema / API / DTO / domain contracts.
4. Accepted ADRs.
5. `TRAVELHUB_CURRENT_CANONICAL_ARCHITECTURE.md`.
6. Commerce lifecycle contract.
7. D4/D4-REM traveler architecture/evidence.
8. Master Debt Register / canonical roadmap.
9. Historical prompts/reports.
10. Assumptions.

Если canonical architecture и repository implementation расходятся:

```text
record discrepancy
```

Не исправлять в audit.

---

# 5. BASELINE

До анализа зафиксировать:

```text
HEAD:
origin/master:
working tree:
git diff --check:
D12 closure SHA:
D13 dependency state:
current TRUE NEXT:
```

Проверить, что:

```text
D12 = CLOSED
TRUE NEXT = D13
```

Если это не подтверждено:

```text
D13 = NOT READY
STOP
```

---

# 6. D4 DEPENDENCY REQUALIFICATION

Roadmap указывает:

```text
D13 dependency = D4
```

Но D13 опирается на более поздние traveler artifacts.

Обязательно проверить:

- D4 closure;
- D4-REM closure;
- Traveler Requirements architecture;
- QuoteTraveler;
- CheckoutIntentTraveler;
- OrderTraveler;
- Passenger;
- Booking snapshot contract;
- final confirmation / booking confirmation semantics.

Не считать D4 "готовым" только по названию stage.

Нужно определить, какие именно D4/D4-REM outputs D13 реально consumes.

---

# 7. TRAVELER / PASSENGER SOURCE AUDIT

Исследовать фактические отношения:

```text
Booking
→ Passenger
→ Traveler-related snapshot
→ OrderTraveler
→ QuoteTraveler
→ CheckoutIntentTraveler
```

Установить:

- какая сущность является canonical Voucher subject;
- какие данные являются historical snapshot;
- какие данные live;
- можно ли использовать Customer;
- можно ли использовать Product;
- можно ли использовать current traveler profile;
- какие данные должны быть immutable at Voucher generation.

### HARD RULE

Не брать Customer как automatic traveler list.

Не брать mutable Product fields как historical traveler authority.

Не использовать live master data для полей, которые должны отражать historical booking state.

---

# 8. VOUCHER PURPOSE

Определить business purpose:

```text
Что подтверждает Voucher?
Для кого он предназначен?
К чему он относится?
Какая commercial/lifecycle state разрешает его выдачу?
Что пользователь должен увидеть?
```

Отдельно различить:

```text
Voucher
≠ Booking
≠ Order
≠ Invoice
≠ Payment receipt
≠ generic Document
```

Если repository/canonical docs определяют только часть semantics — зафиксировать это как gap.

---

# 9. GENERATION POINT

Исследовать:

```text
BookingConfirmed
```

как candidate trigger.

Проверить фактический lifecycle:

```text
BookingRequested
→ ...
→ CONFIRMED
→ Generate Voucher
```

Установить:

- event/command name;
- producer;
- consumer;
- synchronous/asynchronous path;
- idempotency;
- retry;
- duplicate prevention;
- transaction boundary.

Не принимать `BookingConfirmed → Generate Voucher` как полностью реализованный факт только потому, что он присутствует в architecture docs. fileciteturn326file1L329-L342

---

# 10. VOUCHER LIFECYCLE

Выстроить canonical lifecycle, если он существует:

```text
NOT_ISSUED
→ ISSUED
→ REISSUED / REPLACED
→ INVALIDATED / VOID
```

Но **не invent enum values**.

Найти реальные implementation values.

Для каждого state:

```text
meaning
entry condition
terminal?
user-visible?
can regenerate?
can invalidate?
```

Если status model отсутствует:

```text
ARCHITECTURE GAP
```

---

# 11. VERSIONING

Обязательно исследовать document version semantics.

Canonical architecture говорит:

```text
published document version = immutable
correction = new version
```

fileciteturn326file1L341-L342

Для Voucher определить:

```text
voucher identity
document identity
version number
current version
superseded version
published/immutable boundary
```

Проверить, является ли:

```text
VCH-*
```

business identity Voucher, а не document version.

---

# 12. REGENERATION

Audit обязан ответить:

> Что происходит, если после выдачи Voucher меняется Booking?

Исследовать canonical triggers:

- traveler correction;
- service date change;
- passenger change;
- supplier confirmation change;
- booking cancellation;
- other material mutation.

Не принимать regeneration автоматически.

Классифицировать каждый candidate:

```text
REGENERATE
DO NOT REGENERATE
INVALIDATE
NEW VERSION
ARCHITECTURE DECISION REQUIRED
```

---

# 13. INVALIDATION

Исследовать:

```text
Booking cancellation
Booking rejection
service invalidation
manual void
superseded voucher
```

Определить:

- существует ли invalidation;
- immutable ли historical version;
- как current validity определяется;
- кто может invalidate;
- есть ли audit event.

Если business rule отсутствует — не придумывать.

---

# 14. VOUCHER DATA CONTRACT

Составить matrix:

| Field | Source | Historical/Live | Sensitive | Required | Authority |
|---|---|---|---|---|---|

Минимально проверить:

```text
Voucher ID
Booking ID / reference
Order ID / reference
service
service date
supplier/provider
partner
passenger names
passenger count
traveler requirements where applicable
destination/service details
booking status
issue timestamp
version
validity state
```

Но только реально подтверждённые поля считать canonical.

---

# 15. SENSITIVE DATA

Определить, какие данные Voucher содержит:

- traveler personal data;
- contact data;
- passport/identity data;
- date of birth;
- special requirements;
- supplier data;
- pricing;
- payment information.

Для каждого:

```text
included?
masked?
forbidden?
role-dependent?
```

Не включать financial/payment secrets в Voucher без explicit authority.

---

# 16. PERMISSION MODEL

Определить:

```text
who can generate
who can view
who can download
who can reissue
who can invalidate
```

Проверить existing canonical roles:

```text
ADMIN
DIRECTOR
FINANCE
MARKETER
ANALYST
MODERATOR
SALES_MANAGER
OPERATOR
PARTNER
BUYER
```

Не создавать новую role без ADR.

Existing baseline explicitly states these canonical roles and forbids introducing roles such as Booking Agent without architecture change. fileciteturn326file1L258-L283

---

# 17. SCOPE / TENANCY

Проверить:

```text
Platform
Partner
Marketplace
Storefront
Buyer
```

Hard boundary from canonical architecture:

```text
MARKETPLACE
→ TravelHub platform operational/commercial business

STOREFRONT
→ Partner's own customer business
```

Storefront Commerce must not be silently treated as Marketplace commerce. fileciteturn326file4L820-L845

Для Voucher определить:

```text
who owns voucher
who may access it
whether Partner can see Buyer data
whether Platform can see Partner data
```

---

# 18. SECURITY / IDOR

Проверить route/API access model:

```text
/voucher/{id}
```

или фактический route, если уже существует.

Определить:

- identifier;
- API endpoint;
- permission;
- scope resolver;
- direct-link behavior;
- cross-partner protection;
- cross-customer protection.

Особое внимание:

```text
Voucher ID alone
must NOT become authorization.
```

Server must remain authority.

---

# 19. BUSINESS ID

Canonical Phase 3 baseline defines:

```text
Voucher = VCH-*
```

and states that systems do not receive a new business ID without ADR. fileciteturn326file1L241-L256

Проверить:

- current Voucher identifier;
- persistence field;
- uniqueness;
- generation mechanism;
- public/business reference;
- relation to Document ID.

Не вводить второй competing Voucher ID.

---

# 20. DOCUMENT ARCHITECTURE

Проверить существующие:

```text
Document
Template
Voucher
```

и их ownership.

Canonical architecture says Documents owns:

```text
Document
Template
Voucher
```

и Voucher generation is an event/command-driven document operation. fileciteturn326file1L321-L339

Определить:

```text
Voucher entity
→ Document entity
→ Template
→ rendering
→ storage
→ delivery
```

Не дублировать Document subsystem.

---

# 21. RENDERING / FORMAT

Исследовать, что уже существует:

- PDF renderer;
- document generator;
- template engine;
- storage;
- download API;
- preview;
- localization;
- QR/barcode;
- printable format.

Не создавать новый renderer, если canonical Document infrastructure already exists.

Если rendering architecture отсутствует:

```text
record architecture gap
```

не implementation.

---

# 22. TEMPLATE

Проверить:

```text
Template entity
template version
locale
currency
branding
partner data
service data
passenger data
```

Установить:

- есть ли Voucher template contract;
- immutable ли published template;
- может ли template change alter historical voucher;
- versioning.

---

# 23. DELIVERY

Исследовать:

```text
download
print
email
customer cabinet
partner cabinet
staff UI
```

Но не предполагать, что D13 обязан реализовать delivery channels.

Разделить:

```text
required by D13
existing
future
deferred
```

---

# 24. TEMPORAL SEMANTICS

D8 является canonical temporal authority.

Для Voucher проверить минимум:

```text
issuedAt
validFrom
validUntil
serviceDate
cancelledAt
supersededAt
```

если такие fields/events существуют.

Не использовать:

```text
updatedAt
```

как business event timestamp.

Canonical architecture требует:

```text
missing / non-applicable event → "—"
```

и запрещает fabricating timestamps. fileciteturn326file0L139-L179

Если Voucher issue time должен отражать generation event — доказать это source.

---

# 25. COMMERCE REFERENCES

Preserve canonical commerce identity:

```text
MKT-REQ-...
MKT-ORD-...
MKT-BKG-...
MKT-PAY-...
MKT-REF-...
```

Relationships должны использовать:

```text
FK / UUID
```

а не string parsing. fileciteturn326file4L771-L793

Voucher references must not replace Order/Booking identities.

---

# 26. FINANCE BOUNDARY

D13 не реализует Finance.

Voucher не должен автоматически становиться:

```text
Invoice
Payment receipt
Settlement statement
Payout proof
Refund document
```

без explicit canonical evidence.

Проверить, содержит ли Voucher pricing/payment data и почему.

Finance remains separate.

---

# 27. BOOKING / ORDER BOUNDARY

Canonical architecture explicitly separates:

```text
Order ≠ Booking
```

и предусматривает Booking lifecycle distinct from Order. fileciteturn326file0L28-L52

D13 должен определить:

```text
Voucher belongs to:
Booking?
Order?
both as references?
```

Candidate source is Booking, but target authority must be proved.

---

# 28. PARTNER / CUSTOMER / TRAVELER DISTINCTION

Не смешивать:

```text
Customer
Payer
Traveler / Passenger
Partner
Supplier
```

Voucher recipient/subject должен основываться на фактическом booking participant contract.

Нельзя автоматически использовать Customer вместо Passenger/Traveler.

---

# 29. EXISTING IMPLEMENTATION INVENTORY

Исследовать repository по:

```text
Voucher
VCH-
voucher
generate voucher
BookingConfirmed
Generate Voucher
Document
Template
Passenger
OrderTraveler
BookingTraveler
ticket/document
download
pdf
render
```

Для каждого найденного artifact:

```text
implemented
partial
dead code
legacy
planned
```

Особенно искать уже существующие partial implementation, чтобы D13 не создал второй subsystem.

---

# 30. API INVENTORY

Найти existing APIs:

```text
voucher create
voucher issue
voucher regenerate
voucher invalidate
voucher get
voucher download
voucher preview
```

Если API отсутствует:

```text
NOT IMPLEMENTED
```

Не создавать на audit.

Для existing API определить:

```text
path
method
input
output
permissions
scope
versioning
```

---

# 31. UI INVENTORY

Найти:

```text
Voucher button
Voucher tab
Download
Preview
Print
Document list
Booking detail actions
Partner/Buyer surfaces
```

Не считать UI button доказательством working backend.

---

# 32. TEST INVENTORY

Найти existing tests:

```text
Voucher
Document
BookingConfirmed
Passenger
Traveler
PDF
permissions
download
```

Классифицировать:

```text
PASS
FAIL
NOT COVERED
PRE-EXISTING
```

Не менять tests.

---

# 33. IDEAL END-TO-END CHAIN

Построить evidence-backed chain:

```text
Booking lifecycle event
        ↓
Voucher generation command/event
        ↓
source snapshot
        ↓
Voucher persistence
        ↓
Document/template rendering
        ↓
immutable version
        ↓
permission-scoped retrieval
        ↓
download/preview/delivery
```

Для каждого arrow указать:

```text
implemented?
partial?
missing?
```

---

# 34. FAILURE MODES

Исследовать:

- Booking not confirmed;
- missing passengers;
- incomplete traveler data;
- missing service date;
- cancelled Booking;
- invalid Booking ID;
- repeated generation;
- duplicate Voucher;
- renderer failure;
- missing template;
- unauthorized request;
- superseded version;
- stale document;
- sensitive-data leakage.

Для каждого:

```text
handled
partial
unhandled
architecture decision required
```

---

# 35. IDEMPOTENCY

Обязательно определить:

> Что происходит при повторном `BookingConfirmed` или retry генерации?

Нельзя создавать duplicate active vouchers silently.

Проверить existing:

```text
idempotency key
unique constraint
event ID
booking + version uniqueness
```

Если отсутствует:

```text
architecture gap
```

---

# 36. AUDITABILITY

Проверить:

```text
who generated
when generated
which Booking version
which template version
which Voucher version
who invalidated/reissued
```

Не fabricating audit fields.

Если audit infrastructure already exists — использовать его.

---

# 37. DATA CONSISTENCY

Определить consistency invariants.

Примеры для проверки:

```text
Voucher must reference existing Booking
Voucher passenger set must correspond to canonical Booking participants
Voucher version must reference immutable source snapshot
Invalid Voucher cannot be treated as current
Superseded version remains historically retrievable where contract requires
```

Не объявлять invariant canonical, если repository evidence отсутствует.

---

# 38. READINESS CLASSIFICATION

После audit использовать только:

### READY FOR IMPLEMENTATION

Есть достаточно полного canonical contract и implementation path.

### READY WITH EXPLICIT GAPS

Можно безопасно реализовать V1 с ясно ограниченными gaps.

### ARCHITECTURE DECISION REQUIRED

Нет согласованности по source/lifecycle/versioning/access.

### BLOCKED

Не выполнены реальные prerequisites.

---

# 39. REQUIRED REPORT

Создать:

```text
docs/reports/evidence/PHASE_3_D13_VOUCHER_SCOPE_AUDIT.md
```

Структура:

## 1. Executive Summary

## 2. Repository Baseline

## 3. D13 Canonical Definition

## 4. D4/D4-REM Dependency Requalification

## 5. Voucher Purpose

## 6. Source / Participant Authority

## 7. Generation Point

## 8. Voucher Lifecycle

## 9. Versioning

## 10. Regeneration / Invalidation

## 11. Data Contract

## 12. Sensitive Data

## 13. Permission Model

## 14. Scope / Tenancy

## 15. Security / IDOR

## 16. Business ID

## 17. Document Architecture

## 18. Rendering / Template

## 19. Delivery

## 20. Temporal Semantics

## 21. Order / Booking Boundary

## 22. Customer / Payer / Traveler Boundary

## 23. Existing Backend/API/UI Inventory

## 24. Test Inventory

## 25. End-to-End Chain

## 26. Failure Modes

## 27. Idempotency

## 28. Auditability

## 29. Data Consistency

## 30. Dependency Matrix

## 31. Implementation Scope Candidate

## 32. Blocking Decisions

## 33. Readiness Verdict

## 34. Recommended Next Action

---

# 40. DEPENDENCY MATRIX

Обязательно:

| Dependency | State | Blocks D13? | Evidence |
|---|---|---|---|
| D4 | ? | ? | |
| D4-REM | ? | ? | |
| Booking lifecycle | ? | ? | |
| Passenger/Traveler contract | ? | ? | |
| Booking snapshot | ? | ? | |
| Document subsystem | ? | ? | |
| Template subsystem | ? | ? | |
| Permissions | ? | ? | |
| Storage/rendering | ? | ? | |
| D8 | CLOSED | ? | |
| D10 | CLOSED | ? | |
| D11 | CLOSED | ? | |
| D12 | CLOSED | ? | |
| Finance | DEFERRED | ? | |

---

# 41. REQUIRED FINAL BLOCK

В конце report:

```text
D13 READINESS:
CURRENT TRUE NEXT:
FINANCE:
D13 BLOCKERS:
D13 NON-BLOCKING GAPS:
CANONICAL VOUCHER ID:
CANONICAL SOURCE:
CANONICAL GENERATION POINT:
CANONICAL VERSIONING:
CANONICAL INVALIDATION:
CANONICAL PERMISSION:
TEMPORAL AUTHORITY:
DOCUMENT AUTHORITY:
TRAVELER/PASSENGER AUTHORITY:
D10 ↔ D13:
D11 ↔ D13:
D12 ↔ D13:
IMPLEMENTATION SCOPE:
NEXT AUTHORIZED ACTION:
```

---

# 42. GIT REQUIREMENTS

До final verdict:

```bash
git status --short
git diff --check
git log -1 --oneline
git rev-parse HEAD
git rev-parse origin/master
```

Audit-only requirement:

```text
production code changes = 0
```

Допускается только evidence/report artifact.

No unrelated cleanup.

---

# 43. STOP CONDITION

После создания D13 Scope Audit:

**STOP.**

Не делать:

- D13 implementation;
- D14;
- STEP 3.12;
- Finance;
- schema migration;
- API creation;
- UI creation;
- roadmap changes.

Финальный ответ агента должен содержать только:

```text
D13 readiness
+
evidence report
+
blocking decisions
+
recommended next action
```

---

# 44. SUCCESS CRITERIA

```text
[ ] D12 closure verified
[ ] D13 canonical definition identified
[ ] D4/D4-REM dependency requalified
[ ] Voucher source identified
[ ] participant authority identified
[ ] generation point audited
[ ] lifecycle audited
[ ] versioning audited
[ ] regeneration audited
[ ] invalidation audited
[ ] data contract audited
[ ] sensitive data audited
[ ] permissions audited
[ ] scope/tenancy audited
[ ] IDOR/security audited
[ ] business ID verified
[ ] Document/Template architecture audited
[ ] rendering/delivery audited
[ ] temporal semantics audited
[ ] Order/Booking boundary verified
[ ] Traveler/Passenger boundary verified
[ ] API/UI/test inventory completed
[ ] end-to-end chain documented
[ ] failure modes documented
[ ] idempotency audited
[ ] auditability audited
[ ] consistency invariants documented
[ ] dependencies checked
[ ] implementation scope derived
[ ] readiness classified
[ ] evidence report created
[ ] production code unchanged
[ ] STOP
```
